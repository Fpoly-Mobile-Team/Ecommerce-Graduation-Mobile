import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  paginationStyle: {
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
    bottom: 2,
  },
});
